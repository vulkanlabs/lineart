# Vulkan Agent Environment Configuration

# Server Configuration
HOST=0.0.0.0
PORT=8001

# Vulkan Server Configuration
VULKAN_SERVER_URL=http://app:6001

# LLM Provider Configuration
# Note: Set these in your actual environment file, not in version control
DEFAULT_LLM_PROVIDER=openai

# OpenAI Configuration (if using OpenAI)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Anthropic Configuration (if using Anthropic)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-5-haiku-20241022
