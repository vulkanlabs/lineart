# Vulkan Agent Environment Configuration

# Server Configuration
HOST=0.0.0.0
PORT=8001

# Documentation Configuration  
VULKAN_DOCS_PATH=/app/docs

# Database Configuration
VULKAN_DB_PATH=/app/data/vulkan_agent.db

# Note: LLM configuration (API keys, models, parameters) is managed
# through the database and configured via API endpoints, not environment variables.
# See API documentation at /docs for configuration endpoints.
